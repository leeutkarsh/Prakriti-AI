# Core app
streamlit>=1.32

# Webcam capture inside Streamlit (manages the background capture thread
# correctly across Streamlit reruns -- see README "Troubleshooting")
streamlit-webrtc>=0.47
av>=11.0

# Computer vision / hand tracking
opencv-python-headless>=4.9
mediapipe>=0.10.14
numpy>=1.24

# Local viewer bridge server (serves offline_field_viewer/ + GET /gesture)
flask>=3.0
